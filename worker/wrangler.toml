name = "portfolio-chat"
main = "worker.js"
compatibility_date = "2024-09-23"

# Uncomment to restrict to your domain only (recommended for production):
# [triggers]
# routes = [{ pattern = "pymite6941.is-a.dev/*", zone_name = "is-a.dev" }]
